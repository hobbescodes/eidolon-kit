{
  "name": "@eidolonkit/contracts",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "forge build",
    "deps:install": "forge soldeer update",
    "lint": "forge fmt --check && bun solhint \"{script,src,test}/**/*.sol\"",
    "local:deploy": "bun run ./bun-scripts/local.ts",
    "test": "forge test"
  },
  "dependencies": {
    "@eidolonkit/env": "*"
  },
  "devDependencies": {
    "@eidolonkit/typescript-config": "*",
    "solhint": "^5.0.1"
  }
}
