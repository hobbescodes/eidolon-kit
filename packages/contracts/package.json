{
  "name": "@eidolonkit/contracts",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "forge build",
    "soldeer": "forge soldeer update",
    "lint": "forge fmt --check && bun solhint \"{script,src,test}/**/*.sol\"",
    "deploy": "bun run ./bun-scripts/deploy.ts",
    "test": "forge test",
    "wagmi:generate": "wagmi generate"
  },
  "exports": {
    "./wagmi": "./generated/index.ts",
    "./anvil": "./broadcast/Deploy.s.sol/31337/run-latest.json"
  },
  "dependencies": {
    "@eidolonkit/env": "*",
    "@wagmi/cli": "^2.1.10"
  },
  "devDependencies": {
    "@eidolonkit/typescript-config": "*",
    "solhint": "^5.0.1"
  }
}
