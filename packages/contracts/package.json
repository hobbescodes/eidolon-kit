{
  "name": "@eidolonkit/contracts",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "forge build",
    "deps:install": "forge soldeer update",
    "deploy": "bun run ./bun-scripts/local.ts",
    "lint": "forge fmt --check && bun solhint \"{script,src,test}/**/*.sol\"",
    "test": "forge test"
  },
  "dependencies": {
    "@eidolonkit/env": "*"
  },
  "devDependencies": {
    "@eidolonkit/typescript-config": "*",
    "solhint": "^5.0.1"
  }
}
