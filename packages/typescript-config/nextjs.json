// TODO: update this to include ${configDir} when nextjs supports it, remove unnecessary options in web tsconfig afterwards
{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Next.js",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "jsx": "preserve"
  }
}
